[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "minions-agent-tasks"
version = "0.2.0"
description = "gh agent-task prompts, execution tracking, PR references, and review status"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.11"
keywords = ["agent-tasks","ai","minions"]
authors = [
    { name = "Mehdi Nabhani", email = "mehdi@the-mehdi.com" }
]
dependencies = [
    "minions-sdk>=0.2.1",
]

[project.optional-dependencies]
test = ["pytest>=7.0"]

[project.urls]
Homepage = "https://github.com/mxn2020/minions-agent-tasks"
Repository = "https://github.com/mxn2020/minions-agent-tasks"

[tool.hatch.build.targets.wheel]
packages = ["minions_agent_tasks"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
